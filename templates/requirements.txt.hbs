# ckan-extensions requirements

{{#each extensions}}
    # extension: {{this.name}}
    {{#if (eq this.type 'PyPI')}}
        {{this.name}}=={{this.version}}
    {{/if}}
    {{#if (eq this.type 'local')}}
        -e {{this.path}}{{this.name}}
    {{/if}}
{{/each}}
